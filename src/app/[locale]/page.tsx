import { HeroSection } from '@/components/sections/HeroSection'

/**
 * Renders the home page containing the HeroSection component.
 *
 * @returns A JSX element: a <main> element that wraps the HeroSection component.
 */
export default function HomePage() {
  return (
    <main>
      <HeroSection />
    </main>
  )
}